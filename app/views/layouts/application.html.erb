<!DOCTYPE html>
<html xmlns="http://www.w3.org/1999/html">
<head>
  <title>Flat_Test_2</title>
  <%= stylesheet_link_tag "application", controller_path, media: "all" %>
  <%= javascript_include_tag "application", controller_path %>
  <%= csrf_meta_tags %>
</head>
<body>

<div class="navbar navbar-fixed-top">
  <div class="navbar-inner">
    <div class="container-fluid">
      <div class="nav-collapse">
        <ul class="nav">
          <li><%= link_to 'Flat_Test_2', root_path, class: 'brand' %></li>
          <% if user_signed_in? %>
              <li><%= link_to 'Главная', root_path %></li>
              <li><%= link_to 'Добавить событие', new_event_path %></li>
          <% end %>
        </ul>
      </div>
      <% if user_signed_in? %>
          <div class="btn-group pull-right">
            <a href="#" data-toggle="dropdown" class="btn dropdown-toggle">
              <i class="icon-user"></i>
              <%- if current_user.name.present? %>
                  <%= current_user.name %>
              <%- else %>
                  <%= current_user.email %>
              <% end %>
              <span class="caret"></span>
            </a>
            <ul class="dropdown-menu">
              <li><%= link_to 'Профиль', edit_user_registration_path %></li>
              <li><%= link_to 'Выход', destroy_user_session_path, method: :delete, confirm: 'Завершить сеанс?' %></li>
            </ul>
          </div>
      <%- else %>
          <ul class="nav pull-right">
            <li><%= link_to 'Войти', new_user_session_path %></li>
            <li><%= link_to 'Регистрация', new_user_registration_path %></li>
          </ul>
      <% end %>
    </div>
  </div>
</div>

<div class="container-fluid">
  <%= content_for?(:content) ? yield(:content) : yield %>
</div>

</body>

<div class="container-fluid footer">
  <footer class="footer">
    <p>Designed and built with all the love in the world

    <p>
  </footer>
</div>
</html>
